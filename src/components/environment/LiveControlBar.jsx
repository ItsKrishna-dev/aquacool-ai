import{Pause,Play,RotateCcw}from'lucide-react';export default function LiveControlBar({clock}){return <div className="live-controls"><button className="primary" onClick={clock.toggle}>{clock.running?<Pause size={15}/>:<Play size={15}/>} {clock.running?'Pause':'Play'} simulation</button><label>Speed<select value={clock.speed} onChange={e=>clock.setSpeed(Number(e.target.value))}><option value="1">1×</option><option value="5">5×</option><option value="20">20×</option></select></label><button className="secondary" onClick={clock.reset}><RotateCcw size={15}/> Reset</button><span className={clock.running?'live-dot':''}>{clock.running?'SIMULATION RUNNING':'SIMULATION PAUSED'}</span></div>}
