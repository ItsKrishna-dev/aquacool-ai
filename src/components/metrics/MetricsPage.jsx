import Panel from'../common/Panel';export default function MetricsPage({facility,simulation}){const m=simulation.metrics;return <><div className="section-title"><h2>Metrics</h2><p>Operational metrics for {facility.city} automation run.</p></div><div className="metric-list"><Metric label="Queued workloads" value={m.queued}/><Metric label="Completed workloads" value={m.completed}/><Metric label="Blocked workloads" value={m.blocked}/><Metric label="Peak CPU allocation" value={`${m.peakCpu} CPU`}/><Metric label="SLA compliance" value={`${m.slaCompliance}%`}/></div><Panel title="Run summary" tag="CURRENT"><p className="summary-text">The automation engine protects critical workloads, evaluates flexible jobs against capacity and deadlines, then dispatches approved workloads to simulated clusters.</p></Panel></>}
function Metric({label,value}){return <div className="metric-row"><span>{label}</span><strong>{value}</strong></div>}
