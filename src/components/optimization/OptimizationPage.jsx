import Panel from'../common/Panel';import{SlidersHorizontal}from'lucide-react';export default function OptimizationPage({facility,simulation}){return <><div className="section-title"><div><h2>Optimization lab</h2><p>Automation policies and dispatch state for {facility.city}.</p></div><button className="primary" onClick={simulation.runAutomation} disabled={simulation.running}><SlidersHorizontal size={16}/>{simulation.running?'Running…':'Run automation'}</button></div><Panel title="Current automation policy" tag="ACTIVE"><div className="policy-grid"><span>Carbon priority<strong>40%</strong></span><span>Water priority<strong>35%</strong></span><span>Delay penalty<strong>25%</strong></span><span>Capacity<strong>{facility.capacity} CPU</strong></span></div></Panel><Panel title="Dispatch activity" tag="STATEFUL"><div className="dispatch-grid">{simulation.jobs.map(job=><div className="dispatch-card"><b>{job.id}</b><span>{job.name}</span><strong>{job.state}</strong><div>{job.allocatedCpu||job.cpu} CPU / {job.allocatedMemory||job.memory} GB</div></div>)}</div></Panel></>}
