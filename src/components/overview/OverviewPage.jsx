import{Activity,Clock3,Droplets,Leaf,ShieldCheck,Zap}from'lucide-react';import Panel from'../common/Panel';import MetricCard from'../common/MetricCard';import StatusBadge from'../common/StatusBadge';import ProgressBar from'../common/ProgressBar';import AutomationRunConsole from'./AutomationRunConsole';import{formatDeviceTime}from'../../utils/time';
export default function OverviewPage({facility,simulation}){const{metrics,jobs,events,running,runStep}=simulation;return <><div className="overview-hero"><div><div className="section-kicker">AUTOMATION CONTROL PLANE</div><h2>{facility.city} orchestration centre</h2><p>Clock: <b>{new Intl.DateTimeFormat(undefined,{timeStyle:'long'}).format(new Date())}</b></p><div className="snapshot"><span>Scheduler<strong>{running?'EVALUATING':'READY'}</strong></span><span>Queued<strong>{metrics.queued}</strong></span><span>Protected<strong>{jobs.filter(j=>j.state==='LOCKED_CRITICAL').length}</strong></span><span>Capacity<strong>{facility.capacity-metrics.peakCpu} CPU free</strong></span></div></div><div className="run-action"><Zap size={20}/><b>Run scheduling automation</b><p>Ingest workloads, protect critical jobs, select feasible windows and dispatch approved work.</p></div></div><div className="cards"><MetricCard icon={Leaf} label="Carbon reduction" value={`${metrics.carbonReduction}%`}/><MetricCard icon={Droplets} label="Water impact reduction" value={`${metrics.waterReduction}%`} tone="blue"/><MetricCard icon={ShieldCheck} label="SLA compliance" value={`${metrics.slaCompliance}%`} tone="violet"/><MetricCard icon={Clock3} label="Jobs completed" value={metrics.completed} tone="amber"/><MetricCard icon={Activity} label="Dispatching / running" value={`${metrics.dispatching} / ${jobs.filter(j=>j.state==='RUNNING').length}`} tone="slate"/></div>{(running||events.length>0)&&<AutomationRunConsole events={events} running={running} step={runStep}/>}<Panel title="Workload lifecycle" tag="LIVE STATE MACHINE"><div className="lifecycle">{['INGESTED','CLASSIFIED','EVALUATING','APPROVED','DISPATCHED','RUNNING','COMPLETED'].map((state,index)=><div key={state} className={index<runStep?'complete':''}><span>{index+1}</span>{state}{index<6&&<b>→</b>}</div>)}</div></Panel><Panel title="Automation queue" tag={`${jobs.length} WORKLOADS`}><table><thead><tr><th>Workload</th><th>State</th><th>Action</th><th>Allocation</th><th>Progress</th><th>Window</th></tr></thead><tbody>{jobs.map(job=><tr key={job.id}><td><b>{job.id}</b><small>{job.name}</small></td><td><StatusBadge state={job.state}/></td><td>{job.dispatchMessage||job.reason}</td><td>{job.allocatedCpu||job.cpu} CPU / {job.allocatedMemory||job.memory} GB</td><td><ProgressBar value={job.progress||0}/></td><td>{formatDeviceTime(job.recommendedStart||job.baselineStart)}</td></tr>)}</tbody></table></Panel></>}
