import{Play,Sun,Moon}from'lucide-react';export default function Header({page,theme,toggleTheme,onAutomate,running,facility}){return <header><div><div className="eyebrow">INDIA DATA CENTRE • DC-{facility.profile.short}-01</div><h1>{page}</h1><p className="sub">{facility.profile.region} • {facility.profile.renewable} • {new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date())}</p></div><div className="header-actions"><button className="theme-toggle" onClick={toggleTheme}>{theme==='dark'?<Sun size={16}/>:<Moon size={16}/>} {theme==='dark'?'Light':'Dark'} mode</button><button className="primary" onClick={onAutomate} disabled={running}><Play size={16}/>{running?'Automation running…':'Run scheduling automation'}</button></div></header>}
