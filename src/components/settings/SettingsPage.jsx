import Panel from'../common/Panel';export default function SettingsPage({facility,theme,toggleTheme}){return <><div className="section-title"><h2>Settings</h2><p>Facility profile and presentation preferences.</p></div><Panel title="Facility profile" tag="ACTIVE"><div className="settings-grid"><label>City<select value={facility.draftCity} onChange={e=>facility.setDraftCity(e.target.value)}>{['Mumbai','Bengaluru','Delhi','Hyderabad'].map(city=><option key={city}>{city}</option>)}</select></label><label>CPU capacity<input type="number" value={facility.draftCapacity} onChange={e=>facility.setDraftCapacity(e.target.value)}/></label><button className="primary" onClick={facility.applyChanges}>Apply changes</button></div></Panel><Panel title="Appearance" tag="PREFERENCE"><button className="secondary" onClick={toggleTheme}>{theme==='dark'?'Switch to light theme':'Switch to dark theme'}</button></Panel></>}
