export function createEnvironment(profile){return Array.from({length:48},(_,hour)=>{const local=hour%24;const solar=Math.max(0,Math.sin((local-6)*Math.PI/12));const wet=+(22+profile.heat*8*Math.max(0,Math.sin((local-7)*Math.PI/12))).toFixed(1);return{hour,label:`${String(local).padStart(2,'0')}:00`,carbon:+(profile.baseCarbon-220*solar).toFixed(1),wetBulb:wet,waterImpact:+(32+Math.max(0,wet-19)*4.8+profile.waterStress*12).toFixed(1),renewable:+(15+70*solar).toFixed(1)}})}
